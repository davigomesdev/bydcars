import { neutralTheme, purpleTheme, greenLemonTheme, bluishGreenTheme } from './colors';
import { family, weight } from './fonts';
import { sizes } from './measurements';

export const theme = {
	colors: {
		neutralTheme,
		purpleTheme,
		greenLemonTheme,
		bluishGreenTheme,
	},
	font: {
		family,
		weight,
	},
	measure: {
		sizes,
	},
};
